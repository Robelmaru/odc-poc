services:
  app:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - ./backend/.env
    environment:
      - DATABASE_HOST=host.docker.internal
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped

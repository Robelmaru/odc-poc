# ODC Complaint Analyzer - Node.js Version

To install dependencies:

```bash
npm install
```

To run:

```bash
npm start
```

For development with auto-reload:

```bash
npm run dev
```

This is the Node.js version of the ODC Complaint Analyzer, using [Hono](https://hono.dev) with the Node.js adapter.

apiVersion: v1
kind: Service
metadata:
  name: doc-analyzer
  labels:
    app: doc-analyzer
spec:
  type: ClusterIP
  selector:
    app: doc-analyzer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
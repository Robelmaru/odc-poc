apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: doc-analyzer-dev-storage
  labels:
    app: doc-analyzer
    env: development
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  storageClassName: nfs-dynamic
  resources:
    requests:
      storage: 1Gi